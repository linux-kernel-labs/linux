1) In module_init, aloc memorie cu kmap, marchez paginile ca neswapabile si scriu la inceputul fiecarei pagini. In implementarea
functiei "my_mmap", mapez adresa de user-space peste pagina fizica corespunzatoare memoriei alocate anterior.

2) Similar pt vmap, cu mentiunea ca remapez cate o pagina din userspace in memoria fizica, intrucat aceasta nu este alocata contiguu.

3) Cum spatiul de memorie alocat a fost mapat cu permisiunile necesare, se pot efectua operatii atat de read, cat si de write. De asemenea, intrucat spatiul de memorie este mapat peste memoria de kernel si nu de user, s-a putut face transfer folosind memcpy, fara a fi nevoie de copy_to/from_user.

4) In functia ce scrie in fisier, iterez prin lista de vm_area_struct a procesului care citeste si extrag informatiile legate de zonele de mem virtuala alocate.La final, decrementez numarul de referinte la mm_struct pentru a nu ramane "agatata" memoria la terminarea procesului. 
